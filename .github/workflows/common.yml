# File: .github/workflows/common.yml
name: Nightly CI

on:
  push:
    branches: [main]

jobs:
  nightly:
    runs-on: ubuntu-latest

    # Reference the reusable workflow
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: try_cron

      - name: Reuse Print Message
        uses: .github/workflows/actionsMain.yml
